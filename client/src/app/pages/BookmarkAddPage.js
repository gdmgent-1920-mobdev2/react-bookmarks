import React, { } from 'react';
import { SEOSearch } from '../components/frontoffice';

const BookmarkAddPage = ({children}) => {
  return (
    <div>
      <SEOSearch />
    </div>
  );
};

export default BookmarkAddPage;